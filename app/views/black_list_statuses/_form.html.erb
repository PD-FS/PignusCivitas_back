<%= form_with(model: black_list_status, local: true) do |form| %>
  <% if black_list_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(black_list_status.errors.count, "error") %> prohibited this black_list_status from being saved:</h2>

      <ul>
      <% black_list_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
