<%= form_with(model: staff_job, local: true) do |form| %>
  <% if staff_job.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(staff_job.errors.count, "error") %> prohibited this staff_job from being saved:</h2>

      <ul>
      <% staff_job.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
