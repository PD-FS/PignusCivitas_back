<%= form_with(model: person, local: true) do |form| %>
  <% if person.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(person.errors.count, "error") %> prohibited this person from being saved:</h2>

      <ul>
      <% person.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :last_name %>
    <%= form.text_field :last_name %>
  </div>

  <div class="field">
    <%= form.label :first_name %>
    <%= form.text_field :first_name %>
  </div>

  <div class="field">
    <%= form.label :phone %>
    <%= form.text_field :phone %>
  </div>

  <div class="field">
    <%= form.label :identification %>
    <%= form.text_field :identification %>
  </div>

  <div class="field">
    <%= form.label :city_id %>
    <%= collection_select(:person, :city_id, City.all, :id, :name, prompt: true) %>
  </div>

  <div class="field">
    <%= form.label :home_address %>
    <%= form.text_field :home_address %>
  </div>

  <div class="field">
    <%= form.label :office_address %>
    <%= form.text_field :office_address %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
