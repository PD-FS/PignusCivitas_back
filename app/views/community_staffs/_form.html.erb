<%= form_with(model: community_staff, local: true) do |form| %>
  <% if community_staff.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(community_staff.errors.count, "error") %> prohibited this community_staff from being saved:</h2>

      <ul>
      <% community_staff.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :staff_id %>
    <%= form.text_field :staff_id %>
  </div>

  <div class="field">
    <%= form.label :community_id %>
    <%= form.text_field :community_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
