<%= form_with(model: check_staff, local: true) do |form| %>
  <% if check_staff.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(check_staff.errors.count, "error") %> prohibited this check_staff from being saved:</h2>

      <ul>
      <% check_staff.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :entry_date %>
    <%= form.date_select :entry_date %>
  </div>

  <div class="field">
    <%= form.label :exit_date %>
    <%= form.date_select :exit_date %>
  </div>

  <div class="field">
    <%= form.label :notes %>
    <%= form.text_field :notes %>
  </div>

  <div class="field">
    <%= form.label :staff_id %>
    <%= form.text_field :staff_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
